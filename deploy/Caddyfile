# Caddy gets and renews the HTTPS certificate for the domain by itself.
{$DOMAIN} {
	encode zstd gzip

	# Attachments are up to 50 MB
	request_body {
		max_size 60MB
	}

	reverse_proxy app:3000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
